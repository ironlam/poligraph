# dependencies
/node_modules

# plans (local only)
/docs/plans/

# perf baseline (local only)
.perf-baseline.json
/docs/superpowers/

# brainstorm visual companion (local only)
.superpowers/

# storybook
/storybook-static/

# next.js
/.next/
/out/

# production
/build

# env files
.env*
!.env.example

# typescript
*.tsbuildinfo
next-env.d.ts

# prisma generated client
/src/generated/prisma

# testing
/coverage
/tests/visual/test-results/
/tests/visual/playwright-report/

# enrich-batch progress (local tracking)
scripts/enrich-batch-progress.json

# local throwaway scripts (tmp-moderate, ad-hoc batches, etc.)
scripts/.local/

# Moderation preflight artifact (regenerated by cron or manual run)
data/moderation-preflight.json
# Audit de conformité RGPD (artefact local, régénéré à la demande)
data/affairs-compliance-audit.json

# Conviction audit ledger (#566), regenerated locally
data/audit-convictions.json

# Portrait pass report (repair-portraits.ts) — holds the from/to URL pairs a
# rollback would need, regenerated on each run
data/portraits-report.json

# debug / logs
*.log
npm-debug.log*

# OS
.DS_Store
Thumbs.db

# IDE
.idea/
.vscode/
*.swp
*.swo

# secrets / tokens
*.pem
.mcp.json

# vercel
.vercel

# supabase local
/supabase

# claude code
CLAUDE.md
.claude/

# Codex local configuration
/.codex/

# database backups (sensitive data)
/backups
job-logs.txt

# generated tweets
/tweets

# git worktrees
/.worktrees

# Local Search Console analysis (exports + plans with private SEO data, not committed)
/docs/search-console/

# Local imports, generated reports and production backups
/.tmp/
